image: registry.gitlab.io/gitlab-org/cluster-integration/nginx-ingress:2.1

stages:
  - deploy

pages:
  stage: deploy
  script:
    - mkdir .public
    - cp -r * .public
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - main